# HTML/CSS Style Sampler
A sample of common HTML elements to visualize CSS styles

## Setup
- Drop your SASS files in the `styles/` directory
- Run `npm install && gulp build`
- Navigate to `localhost:9000` in your browser
